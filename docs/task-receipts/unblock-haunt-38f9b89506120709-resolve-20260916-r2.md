# Haunt unblock resolution — 2026-09-16

Task: `unblock/haunt/38f9b89506120709/resolve`

## Recovery

- Stopped the two unrelated resident models that blocked the run:
  `ollama stop qwen3-vl:4b-instruct` and `ollama stop gemma4:e2b-it-qat`.
- Ran the exact one-repetition zy establishment command, using a new append-only
  tape to preserve the earlier establishment-3 result:
  `runs/tiny-fleet-v1_zy_h2-establishment-4.jsonl`.
- The run exited 0 and produced one complete repetition, but it was
  `NOT-ESTABLISHED`: the model broke at turn 1, obeyed 0/8 spoken turns, and
  made 8/8 calls. This is not an H2 verdict.
- Replay exited 0 with the same `NOT-ESTABLISHED` result. Tape SHA-256:
  `22c3d7d7a918e6cc52f3181f9d63a7f8aa6d32d1fd84b31cc40d27b77474baba`.

## Root cause of the recurring resident model

The fresh model was loaded by the exact `cryptohaunt run` command above. Its
Ollama provider in `cryptohaunt/providers.py` calls `/api/chat` without a
`keep_alive` field, so Ollama retains the model under its default idle lifetime
after the run. `ollama stop tiny-fleet-v1:latest` removed the test model and its
`llama-server`; subsequent `ollama ps` showed only the unrelated pre-existing
`qwen3-vl:4b-instruct` and `all-minilm:latest` services.

## Verification

- `python3 -m unittest discover -s tests -q`: 57 tests, OK.
- `python3 -m cryptohaunt selftest`: OK (included in the chained verification).
- `python3 -m cryptohaunt replay runs/tiny-fleet-v1_zy_h2-establishment-4.jsonl`: exit 0.
- Final `ollama ps`: no `tiny-fleet-v1:latest` resident.

## Live reloader shutdown

The repeated model loading was traced beyond the experiment itself:

- `mesh-cam-watch.service` invoked `mesh-face-recognize` and
  `mesh-misha-eye-contact`, both defaulting to `qwen3-vl:4b-instruct`.
- `mesh-imac-cam-watch.service` invoked `mesh-object-id`/`mesh-face-recognize`.
- Root cron also ran `mesh-face-recognize` every minute and `mesh-misha-wake`
  every three minutes.
- `mesh-room-reflex.service` independently loaded `qwen2.5:3b-8k` through
  `mesh-room-address`.

Disabled the three services, removed the two vision cron entries, and marked
`scripts/mesh-face-recognize` and `scripts/mesh-misha-wake` with
`reflex-cadence: off` so mesh-autowire cannot recreate them. Final `ollama ps`
is empty.
